package com.hlx.view.common;

/**
 * All the frame should implements this interface so that
 * can dynamically load the page
 * @author hlx
 * @version 1.0 2018-3-18
 */
public interface Renewable {

    void refresh();
}
